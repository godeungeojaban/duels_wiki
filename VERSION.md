# Duels Wiki 3.63

## 3.63

- Duels 기본 참조 메뉴에서 `이름`과 캐릭터 기본 `스태미나`를 제거했다. 기술별 스태미나 소모/회복 등 실제 변동 수치는 기술 참조에 그대로 남긴다.
- 새 참조 키 표기를 `캐릭터타입` → `스타일`, `교전사거리` → `사거리`, `이동속도단계` → `이동속도_단계`, `난이도별` → `난이도_별`로 정리했다.
- UI와 자동 생성 명령은 새 키만 사용한다. 원본 `Duels.html`의 다양한 내부 필드명을 읽기 위한 파서 별칭은 유지하며, 과거 형태를 일부러 실패시키는 차단 코드는 추가하지 않았다.

## 3.62

- Duels 참조의 기술 추출기를 다시 작성해 `skill` 키를 기술 컨테이너로 오인하여 실제 스킬이 누락되던 문제를 수정했다.
- 기술 객체의 하위 `hit`, `effects`, `buff`, `debuff`, `status`, `values` 등을 재귀적으로 탐색해 피해량, 스태미나 소모량, 쿨다운, 사거리, 버프/디버프 세기와 지속시간, 넉백, 선/후딜레이를 추출한다.
- 중첩 객체 파싱 깊이를 확장해 `debuff -> slow -> duration`처럼 깊게 들어간 수치가 문자열로 뭉개지지 않도록 했다.
- Duels 참조 메뉴의 기본 캐릭터 항목에서 공격력/방어력을 제거하고, 실제로 발견된 기술 전투 수치를 기술 그룹에 표시한다.
- Duels 분석 캐시에 파서 버전을 기록해 이전 버전이 만든 불완전한 캐시를 새 파서에서 자동 재사용하지 않도록 했다.

## 3.61

- 문서 URL 진입 시 전체 사이드바 인덱스와 Duels 원본 분석을 먼저 기다리던 직렬 초기화 구조를 제거했다. 문서 JSON을 최우선으로 불러와 즉시 렌더링한 뒤 사이드바 인덱스를 유휴 시점에 로드한다.
- 문서에 Duels 참조가 없으면 `/api/duels-data`를 전혀 호출하지 않는다. 참조가 있는 문서도 본문을 먼저 표시하고 참조 위치를 임시 자리표시자로 렌더링한 뒤 원본 데이터가 준비되는 즉시 해당 값만 치환한다.
- Duels 데이터 요청을 Promise 단위로 중복 제거해 한 화면에서 여러 참조가 있어도 원본 조회 요청은 한 번만 발생한다.
- 최근에 분석한 `Duels.html` 카탈로그의 디스크 캐시를 런처 재시작 후에도 기존 TTL 범위 안에서 즉시 재사용하도록 최적화했다.
- 버전 확인 요청도 문서 표시를 막지 않도록 비동기화했다. 기능과 저장 데이터 형식은 변경하지 않았다.

## 3.60

- Duels 참조 메뉴에서 `스킬.1.이름`처럼 내부 배열 경로와 원본 필드 키를 직접 노출하던 방식을 제거했다.
- 캐릭터의 기술을 `평타`, `스킬`, `반격기` 등 실제 기술 단위로 묶어 표시하고, 해당 기술에서 실제로 발견된 전투 수치만 참조 메뉴에 노출한다.
- 기술 참조 항목으로 피해량, 스태미나 소모량, 쿨다운, 사거리, 버프/디버프 세기와 지속시간, 일반 효과 세기/지속시간, 넉백, 선딜레이, 후딜레이를 지원한다. 값은 `Duels.html`에서 읽으며 캐릭터별 수치를 하드코딩하지 않는다.
- `이동속도단계` 참조를 추가했다. 원본에 텍스트 속도 등급이 있으면 이를 우선하며, 없으면 `Duels.html`에서 읽은 전체 캐릭터 이동속도 수치 분포를 기준으로 `매우 느림 / 느림 / 보통 / 빠름 / 매우 빠름`의 5단계로 계산한다. 기존 `이동속도` 숫자 참조는 그대로 유지한다.
- `난이도별` 참조를 추가해 원본 난이도 숫자를 `★` 1~5개로 표현할 수 있게 했다. 기존 `난이도` 숫자 참조도 유지한다.
- 캐릭터 `칭호` 참조를 추가했다. `epithet`, `nickname`, `subtitle`, `characterTitle`, `title`, `칭호` 등 원본 필드를 자동 탐색한다.
- 설명, 배경, 소개, 스킬 설명 같은 문장형 필드는 계속 참조 대상에서 제외한다.
- 참조 함수의 공식 표기는 기존과 동일하게 `{{=duels("캐릭터","필드")}}`이며 함수명은 대소문자를 구분하지 않는다.

## 3.59
- 듀얼즈 참조 창의 캐릭터 목록과 가져올 값 목록을 완전히 분리. 캐릭터 목록은 실제 캐릭터 프리셋과 원본 데이터가 매칭되는 항목만 표시해 능력치/필드명이 캐릭터처럼 섞이는 문제를 제거.
- 가져올 값을 `input + datalist` 자동완성에서 명시적인 선택 메뉴로 변경. 기본 능력치, 원본 능력치, 스킬 필드를 그룹별로 표시하며 체력 외 항목이 보이지 않던 문제를 수정.
- 기본 참조 메뉴에 이름, 이미지, 체력, 스태미나, 이동속도, 난이도, 공격력, 방어력, 캐릭터타입, 교전사거리, 역할군을 항상 제공하고, 원본에서 탐지된 추가 능력치와 스킬 필드를 뒤에 확장.

## 3.58
- 표 셀 드래그 다중 선택을 문서 전역 pointermove 기반으로 재구현. 첫 드래그부터 표를 선택하고 사각형 범위를 안정적으로 확장한다.
- 캐릭터 색상/이미지 프리셋을 눌러도 현재 표 및 셀 선택 상태가 해제되지 않도록 선택 해제 조건을 수정.
- Duels 참조 함수의 공식 문법을 `{{=duels("캐릭터","필드")}}`로 통일. 함수명은 대소문자 비구분으로 해석하되 UI/문서는 소문자 `duels`를 사용한다.
- 3.55~3.56의 백틱 함수 문법은 제거. 백틱은 인라인 이미지 병기 용도로만 사용한다.
- 표/셀 단일 클릭은 요소 선택만 수행하고, 요소 옆 공백 클릭만 표 오른쪽 아래 본문 커서를 생성하도록 선택 모델을 분리.
- 표 선택 상태에서 드래그로 셀 다중 선택. 비편집 상태에서 `Backspace`/`Delete` 시 선택 셀의 내용만 일괄 삭제.
- 셀 더블클릭 시 표/셀 선택을 유지하면서 본문과 같은 직접 텍스트 편집 모드로 전환. 편집 중 삭제 키는 일반 텍스트 편집 규칙을 따른다.

## 3.56
- Duels 참조 신규 문법 `{{=DUELS(...)}}` 도입, 기존 백틱 참조 읽기 호환.
- 캐릭터 타입 / 교전 사거리 / 역할군 개별 참조와 장문 설명 필드 제외.
- 표 셀 장문 자동 개행 및 본문 직접 셀 편집.
- 표 전용 리본과 드래그 다중 셀 선택 도입, 행/열 일괄 선택 UI 제거.

## 3.55

- `LyangNem/Duels`의 `Duels.html`을 원본으로 사용하는 캐릭터 데이터 참조 기능 추가. 캐릭터명/이미지/능력치/스킬 정보를 Wiki 문서에 복사하지 않고 렌더 시점에 원본을 해석한다.
- 백틱 인라인 문법 확장: `` `=DUELS("캐릭터","필드")` `` 또는 `` `=캐릭터("캐릭터","필드")` `` 형식을 일반 텍스트와 함께 사용할 수 있다. 기존 백틱 이미지 URL 문법은 그대로 유지한다.
- `이름`, `이미지`, `체력`, `스태미나`, `이동속도`, `난이도`, `공격력`, `방어력`, `설명` 별칭과 `스킬.N.이름`, `스킬.N.피해`, `스킬.N.쿨다운` 형식 지원. 원본 객체의 실제 필드명도 직접 조회할 수 있으며 `필드목록`, `스킬목록` 진단 명령을 제공한다.
- 삽입 리본에 `듀얼즈 참조` 도구 추가. 최신 원본에서 인식한 캐릭터/필드를 선택하고 명령식을 현재 커서 위치에 삽입할 수 있으며 원본 강제 새로고침도 제공한다.
- 로컬 런처에 `/api/duels-data` 추가. 공개 GitHub 원본을 읽어 캐릭터 객체를 구조적으로 탐색하고, 5분 메모리 캐시와 로컬 파생 캐시를 사용한다. 최신 원본 조회 실패 시 이전 파생 캐시가 있으면 읽기 전용 fallback으로 사용한다.
- 정적 Wiki 빌더도 동일한 원본 해석기와 명령 문법을 사용하므로 본문, 제목, 각주, 표 셀에 삽입된 참조를 동일하게 렌더링한다.
- 참조 해석 실패 시 문서 원문을 변형하거나 삭제하지 않고 열람 화면에 `[참조 오류]`만 표시한다.

## 3.54

- 문서 제목과 목차 블록 제목(섹션 제목)에도 각주 참조를 삽입할 수 있도록 제목 편집 영역을 각주 대응 인라인 편집기로 변경. 문서 파일명/URL/사이드바 및 목차에는 각주 표기를 제외한 순수 제목을 사용해 기존 경로 구조를 유지한다.
- 제목에 표시되는 각주는 일반 본문 각주와 동일하게 위첨자 `[번호]`, 호버 미리보기, 클릭 이동을 사용한다.
- 표 편집창의 셀 텍스트 도구에 `각주`를 추가. 현재 셀의 커서 위치에 기존 각주를 참조하거나 새 각주를 생성해 삽입할 수 있으며, 표 편집창을 닫지 않아 선택 범위와 미저장 표 내용이 유지된다.
- 표 셀의 각주 뒤에서 입력할 때도 일반 본문과 같은 커서 보정 로직을 적용해 위첨자가 이어지는 문제를 방지한다.
- 각주 삭제 시 표 구성요소의 저장 payload 안에 남아 있는 해당 각주 참조까지 함께 제거하도록 보강.
- 정적 Wiki 빌더가 문서/섹션 제목과 표 셀을 포함한 모든 각주 참조 번호와 앵커를 현재 각주 배열 기준으로 다시 동기화하도록 변경.

## 3.53
- 각주 뒤 입력 시 위첨자가 이어지는 문제를 0폭 문자 대신 selection 보정 방식으로 수정.
- 새 각주 삽입 직후뿐 아니라 저장 후 재열기한 기존 각주에서도, 각주 끝 경계에서 입력하면 커서를 일반 본문 텍스트 노드로 이동.
- 각주 앞쪽 경계는 건드리지 않아 앞에 입력하려는 동작은 유지.


## 3.52
- 각주 참조 뒤에서 입력할 때 위첨자 서식이 계속 이어지는 문제를 수정.
  - 삽입 직후 일반 본문용 0폭 커서 지점을 만들고 저장 시 해당 표식만 제거하도록 변경.
- 각주 블록에서 번호 열 너비와 번호-본문 간격을 축소.
- 각주 미리보기 팝오버의 제목에서 `각주` 문구를 제거하고 `[번호]`만 표시.
- 브라우저 기본 `title` 툴팁(`각주 1`)을 제거해 커스텀 미리보기와 중복 표시되지 않도록 수정.
  - 이전 버전에서 저장된 자동 생성 `title`도 정적 빌드 시 제거.

## 3.51
- 각주 삽입 창을 여는 순간 편집기의 선택 영역이 사라져 각주 참조를 삽입하지 못하던 문제 수정. 각주 창을 열기 직전에 본문 Range와 대상 편집 영역을 별도로 보관하고, 기존 각주 선택 또는 새 각주 생성 후 해당 위치를 복원해 정확한 커서 위치에 위첨자 참조를 삽입한다.
- 각주 삽입용 Range는 일반 리본/모달 포커스 변경과 분리해 관리하며, 삽입 완료 또는 취소 시 정리하도록 변경했다.

## 3.50
- 각주 블록을 지연 생성 방식으로 정리. 각주가 없는 기존 문서에는 각주 블록을 만들지 않고, 첫 새 각주가 추가되는 순간에만 문서 최하단의 고정 `각주` 블록을 생성한다.
- 각주 블록의 각 항목에 개별 `삭제` 버튼 추가. 각주 삭제 시 본문에 존재하는 해당 위첨자 `[번호]` 참조도 함께 제거하고 남은 각주 번호를 자동으로 다시 매긴다.
- 마지막 각주를 삭제해 문서의 각주가 0개가 되면 고정 `각주` 블록도 즉시 제거된다. 저장 후 정적 위키에서도 각주가 없는 문서에는 각주 영역이 출력되지 않는다.

## 3.49
- 각주 참조에 미리보기 팝오버 추가. PC에서는 `[번호]`에 마우스를 올리면 해당 각주 내용을 즉시 확인할 수 있고, 클릭하면 기존처럼 문서 하단의 각주 항목으로 이동한다.
- 터치 환경에서는 첫 탭으로 각주 팝오버만 열고 선택 상태를 표시하며, 같은 각주 참조를 두 번째로 탭할 때만 하단 각주로 이동하도록 변경해 스크롤 중 오작동을 방지했다.
- 모바일 각주 팝오버에 `각주로 이동` 버튼과 닫기 버튼을 제공하고, 바깥 영역을 터치하면 닫히도록 처리했다.
- 각주 팝오버는 편집기 열람 화면과 GitHub Pages 정적 사이트에 동일한 디자인과 동작으로 적용된다.

## 3.48

- 편집 리본의 삽입 탭에 `각주` 도구 추가. 본문 커서 위치에 위첨자 `[번호]` 형태의 각주 참조를 삽입하며 해당 문서 최하단 각주 항목으로 이동한다.
- 각주 삽입 창에서 기존 각주를 다시 참조하거나 새 각주를 생성할 수 있으며, 새 각주는 생성 순서대로 번호를 부여한다.
- 문서 최하단에 고정 `각주` 블록 추가. 일반 섹션과 구분되는 비번호 고정 제목을 사용해 목차에는 포함되지 않으며 제목 수정 및 블록 삭제 UI를 제공하지 않는다.
- 각주 블록의 각 항목 내용은 직접 편집 가능하며 저장 데이터의 각주 본문과 즉시 동기화된다. 동일 각주를 여러 위치에서 참조해도 하나의 각주 내용을 공유한다.
- 정적 열람 사이트에도 각주 블록, 위첨자 링크, 앵커 이동과 동일한 디자인을 반영.

## 3.47

- 패키지의 `repository/` 중간 폴더를 제거하고 ZIP 최상위 폴더 자체를 GitHub 저장소 루트로 변경. `DuelsWikiEditor.py`, `editor/`, `wiki/`, `media/`, `site/`, `.github/`, 동기화 스크립트와 문서를 모두 같은 루트에 배치한다.
- `run_editor.bat`은 ZIP 최상위에 유지하되 `.gitignore`에 추가하여 로컬 실행/런처 업데이트 전용 파일로 사용하고 GitHub에는 올리지 않도록 변경.
- `git_sync.bat` / `git_sync.sh`는 새 최상위 구조에서 바로 실행되며, 기존과 동일하게 원격 `wiki/`와 `media/`를 우선 복원해 사용자 문서와 이미지를 보호한다.
- 런처의 GitHub 에디터 안내 경로와 README의 저장소 구조 설명을 새 루트 구조에 맞게 정리.

## 3.46

- 일반 문서 화면에 `문서 복제`를 추가. 원본 내용과 구성요소를 그대로 복사하면서 새 문서명과 대상 카테고리만 지정해 별도 문서로 생성한다.
- `DuelsWikiEditor.py`를 저장소 제품 파일에 포함하고 `git_sync.bat` / `git_sync.sh`가 함께 commit하도록 변경.
- `run_editor.bat`이 GitHub `main`의 `DuelsWikiEditor.py`를 내려받아 `APP_VERSION`을 비교하고, 원격 버전이 더 높을 때 로컬 런처를 교체한 뒤 실행하도록 변경. 업데이트 확인 실패 시 기존 로컬 런처로 계속 실행한다.
- 표에 `너비 맞춤` 토글 추가. 저장된 기본 너비/열 너비 값은 변경하지 않고 기존 열 비율을 기준으로 편집 블록 가로 폭에 맞추며, 해제 시 원래 픽셀 너비로 복귀한다. 정적 열람 사이트에도 동일한 동작을 적용.
- 표 편집 창에 행 전체선택/열 전체선택/전체 해제 UI 추가. 병합 셀은 해당 행·열과 실제로 겹치는 원본 셀 기준으로 선택된다.
- 체크박스로 다중 선택한 셀들의 배경색을 한 번에 적용하는 기능 추가.
- `열 균등` 기능 추가. 병합 셀의 표시 개수가 아니라 병합 전 원래 열 개수를 기준으로 열 너비를 동일하게 배분하며 표의 기본 가로 크기는 유지한다.
- `token.txt`가 존재하면 기존 `config.json` 토큰 유무와 관계없이 `token.txt`를 우선 적용하고, `config.json` 저장 성공 후 `token.txt`를 삭제하는 정책으로 복구.

## 3.45

- 에디터 웹페이지의 버전 표시는 `editor/version.json`을 단일 기준으로 읽어 표시하도록 변경. 하드코딩된 과거 버전 표시 제거.
- 런처 버전은 `APP_VERSION` 하나를 기준으로 HTTP Server 헤더와 콘솔 표시에 공통 적용.
- `run_editor.bat`의 하드코딩 버전 문구를 제거해 이후 패키지 버전과 어긋나지 않도록 변경.
- Git 동기화 배너와 Editor README의 현재 버전 표기도 3.45로 동기화.

## 3.44

- `config.json`에 이미 GitHub Token이 저장되어 있으면 `token.txt`의 내용을 읽지 않고 즉시 삭제하도록 변경.
- `config.json`에 Token이 없을 때만 `token.txt`를 읽어 Token을 저장하고, 저장에 성공한 뒤 파일을 삭제한다. 읽기/저장 실패 시에는 재시도를 위해 파일을 유지한다.

## 3.43

- `token.txt`를 상시 토큰 저장 파일이 아닌 일회성 가져오기 파일로 변경. 에디터 실행 시 파일이 있으면 기존 저장 토큰 유무와 관계없이 파일의 토큰을 우선 가져와 `config.json`에 저장한다.
- `token.txt`에서 토큰을 읽고 로컬 설정 저장까지 성공하면 파일을 즉시 삭제한다. 읽기/저장 실패 시에는 삭제하지 않아 다음 실행에서 재시도할 수 있다.
- 에디터는 더 이상 `token.txt`를 자동 생성하거나 GitHub 설정에서 저장한 토큰을 `token.txt`에 동기화하지 않는다.

## 3.42

- GitHub 동기화에서 `README.md`를 `VERSION.md`와 마찬가지로 명시적으로 stage하고, 내용 변경 시 staging 여부를 로그로 확인하도록 변경.
- 첫 실행 시 실행 파일 옆에 자동 생성되는 `token.txt`를 로컬 PAT 저장 파일로 추가. 로컬 `config.json`에 토큰이 없으면 시작 시 `token.txt`를 자동으로 읽어 인증하고 로컬 설정에도 복사.
- GitHub 설정에서 새 토큰을 저장하면 `token.txt`도 갱신하며, 토큰 입력란을 비운 채 저장소 설정만 수정해도 기존 토큰이 지워지지 않도록 수정.

## 3.41

- GitHub 동기화 시 `VERSION.md`를 제품 파일과 별도로 명시적으로 stage하고, 커밋 전에 staging 상태에 포함되었는지 검증하도록 강화.
- Windows `git_sync.bat`과 POSIX `git_sync.sh` 모두 동일하게 `VERSION.md`를 반드시 동기화 대상으로 취급.


## 3.40

- 표 외곽선 두께를 3.37 수준의 1px로 되돌림. 셀 내부 경계선은 기존 1px 유지.
- 백틱(`) 두 개 사이에 이미지 URL을 입력하면 열람 화면에서 글 흐름 안의 인라인 이미지로 렌더링하는 문법 추가. 지원: http(s), /media/, /__media__/ 경로와 png/jpg/jpeg/webp/gif/svg. 편집 화면에서는 원문 문법을 그대로 표시해 수정 가능.

- 표 편집창의 셀을 contenteditable 기반으로 전환하고 공용 미니 서식 툴바(굵게/기울임/밑줄/취소선/글자색/배경색/정렬/내부 링크/서식 제거)를 추가.
- 셀 내부 경계선은 중립색으로 다시 표시하고, 사용자가 지정하는 테두리 색상은 표 전체 외곽선에만 적용.
- 새 셀의 기본 텍스트 정렬을 중앙으로 변경.

- 표 셀별 좌/중앙/우측 정렬 추가.
- 표 글꼴을 본문 UI 글꼴과 동일하게 통일.
- 테두리 색상은 표 외곽선에만 적용하고 셀 테두리는 제거.
- 실제 크기→기본 크기 동기화 시 각 열의 실제 렌더 폭을 개별 저장하여, 내용 때문에 늘어난 열만 커지고 다른 열 폭은 유지.

- 표 구성요소 추가. 삽입 시 기본 가로 너비(기본 프리셋 400px), 행/열 수, 테두리 색상을 지정 가능. 높이는 직접 지정하지 않으며 한 줄 높이를 기본으로 사용.
- 표는 설정된 기본 너비를 최소 크기로 유지하고, 셀의 한 줄 내용이 길어지면 자동으로 실제 가로 너비가 늘어남. 명시적 개행만 셀 높이를 늘림.
- 표 편집 창에서 셀별 배경색/텍스트, 열 최소 너비, 행/열 추가·제거, 사각형 셀 병합/병합 해제 지원.
- 현재 실제 표 너비를 기본 너비로 동기화하는 기능 추가. 열 너비 조정 시 기본 너비도 열 최소 너비 합계로 갱신.
- 표도 캐릭터 카드/설명 상자와 동일하게 한 번 클릭 또는 같은 높이 여백 클릭 시 오른쪽 아래 편집 커서를 활성화하고, 더블클릭 시 전용 편집 창을 엶.

## 3.34
- 글머리/번호 버튼이 선택 범위를 강제로 블록 경계까지 확장하면서 Chromium에서 목록 명령이 무시되던 문제 수정. 실제 선택 범위를 유지한 채 브라우저의 줄 단위 목록 명령을 실행하도록 변경.
- 목록 적용/해제 뒤 생길 수 있는 잘못된 중첩 `<p><ul>...</ul></p>` 및 루트 텍스트를 정상 블록 구조로 자동 정리.

- 편집기와 정적 열람 사이트의 시각 디자인을 3.15 기준으로 되돌림.
- Duels 3.1717의 캐릭터 설명/툴팁과 동일한 UI 폰트 스택(SUIT → Pretendard → Wanted Sans → Noto Sans KR 등)을 편집기와 열람 사이트에 적용.
- 3.17의 GitHub PUT/DELETE 409 stale SHA 자동 재시도 로직 유지.
- 카테고리 정렬 시 미분류를 항상 가장 아래에 배치.
- 3.15 디자인을 유지하면서 모바일 폭 넘침/이미지/편집 폼 레이아웃 안정화.
- 표 기능은 계속 제거된 상태 유지.



## 3.34
- 글머리/자동 번호 매기기 버튼이 문자 선택 범위가 아니라 현재 줄 단위로 적용되도록 변경. 여러 줄을 선택했을 때만 선택한 줄들을 함께 목록으로 변환.
- 업데이트 Git 동기화에서 `wiki/`와 `media/`를 제품 파일과 분리. 기존 GitHub 저장소가 있으면 원격의 위키 문서/미디어를 먼저 복원하고 에디터·정적 사이트·워크플로·동기화 스크립트만 교체/커밋하도록 변경.
- 새 저장소를 처음 만드는 경우에만 기본 `wiki/_root.json`, `wiki/categories.json`, `media/images/.gitkeep`을 초기 데이터로 추가.

## 3.32
- 홈 > 단락에 Word식 가운데점 글머리 목록과 자동 번호 목록을 명확히 제공. Enter 시 다음 항목이 자동 생성되며 빈 항목에서 Enter를 다시 누르면 목록을 종료.
- 편집기와 정적 열람 사이트에서 가운데점 목록/자동 번호 목록의 표시 스타일을 통일.
- 목차의 번호 영역에 고정 폭을 주던 방식을 제거하고 깊이별 들여쓰기 + 고정 간격으로 변경해 4.1 / 4.1.1 같은 하위 목차에서 제목 앞 공백이 비정상적으로 커지는 문제 수정.


## 3.31
- 캐릭터 카드와 설명 상자를 한 번 클릭하면 해당 요소 바로 아래의 실제 텍스트 문단을 활성화하도록 변경.
- 요소 바로 아래에 이미 빈 입력 문단이 있으면 중복 개행을 만들지 않고 기존 문단을 재사용.
- 캐릭터 카드 편집을 설명 상자와 동일하게 더블클릭으로 열도록 변경.
- 더블클릭 시 앞선 단일 클릭 처리를 취소해 빈 문단이 생긴 뒤 편집창이 열리는 현상을 방지.

## 3.20

- 캐릭터 카드 구성요소를 프로필 폼이 아닌 범용 이미지+텍스트 카드로 단순화.
- 원형 초상화/스탯/스킬/칭호/호버 툴팁/호버 확대 효과 제거.
- 듀얼즈 카드처럼 이미지가 카드 배경 전체를 채우고 아래쪽으로 어두운 그라데이션을 적용.
- 기존 3.19 카드 데이터는 열 때 범용 텍스트 카드로 자동 변환.

## 3.19
- 삽입 리본에 듀얼즈식 캐릭터 카드와 설명 상자 구성요소 추가.
- 캐릭터 카드는 초상화/이름/대표색/체력/이동속도/설명/스킬 설명을 저장하고 hover/focus 설명 툴팁을 표시.
- 편집 화면에서 구성요소 더블클릭으로 재편집/삭제 가능.
- 정적 열람 사이트에도 동일한 구성요소 스타일 적용.
