# Duels Wiki 3.44

## 3.44

- `config.json`에 이미 GitHub Token이 저장되어 있으면 `token.txt`의 내용을 읽지 않고 즉시 삭제하도록 변경.
- `config.json`에 Token이 없을 때만 `token.txt`를 읽어 Token을 저장하고, 저장에 성공한 뒤 파일을 삭제한다. 읽기/저장 실패 시에는 재시도를 위해 파일을 유지한다.

## 3.43

- `token.txt`를 상시 토큰 저장 파일이 아닌 일회성 가져오기 파일로 변경. 에디터 실행 시 파일이 있으면 기존 저장 토큰 유무와 관계없이 파일의 토큰을 우선 가져와 `config.json`에 저장한다.
- `token.txt`에서 토큰을 읽고 로컬 설정 저장까지 성공하면 파일을 즉시 삭제한다. 읽기/저장 실패 시에는 삭제하지 않아 다음 실행에서 재시도할 수 있다.
- 에디터는 더 이상 `token.txt`를 자동 생성하거나 GitHub 설정에서 저장한 토큰을 `token.txt`에 동기화하지 않는다.

## 3.42

- GitHub 동기화에서 `README.md`를 `VERSION.md`와 마찬가지로 명시적으로 stage하고, 내용 변경 시 staging 여부를 로그로 확인하도록 변경.
- 첫 실행 시 실행 파일 옆에 자동 생성되는 `token.txt`를 로컬 PAT 저장 파일로 추가. 로컬 `config.json`에 토큰이 없으면 시작 시 `token.txt`를 자동으로 읽어 인증하고 로컬 설정에도 복사.
- GitHub 설정에서 새 토큰을 저장하면 `token.txt`도 갱신하며, 토큰 입력란을 비운 채 저장소 설정만 수정해도 기존 토큰이 지워지지 않도록 수정.

## 3.41

- GitHub 동기화 시 `VERSION.md`를 제품 파일과 별도로 명시적으로 stage하고, 커밋 전에 staging 상태에 포함되었는지 검증하도록 강화.
- Windows `git_sync.bat`과 POSIX `git_sync.sh` 모두 동일하게 `VERSION.md`를 반드시 동기화 대상으로 취급.


## 3.40

- 표 외곽선 두께를 3.37 수준의 1px로 되돌림. 셀 내부 경계선은 기존 1px 유지.
- 백틱(`) 두 개 사이에 이미지 URL을 입력하면 열람 화면에서 글 흐름 안의 인라인 이미지로 렌더링하는 문법 추가. 지원: http(s), /media/, /__media__/ 경로와 png/jpg/jpeg/webp/gif/svg. 편집 화면에서는 원문 문법을 그대로 표시해 수정 가능.

- 표 편집창의 셀을 contenteditable 기반으로 전환하고 공용 미니 서식 툴바(굵게/기울임/밑줄/취소선/글자색/배경색/정렬/내부 링크/서식 제거)를 추가.
- 셀 내부 경계선은 중립색으로 다시 표시하고, 사용자가 지정하는 테두리 색상은 표 전체 외곽선에만 적용.
- 새 셀의 기본 텍스트 정렬을 중앙으로 변경.

- 표 셀별 좌/중앙/우측 정렬 추가.
- 표 글꼴을 본문 UI 글꼴과 동일하게 통일.
- 테두리 색상은 표 외곽선에만 적용하고 셀 테두리는 제거.
- 실제 크기→기본 크기 동기화 시 각 열의 실제 렌더 폭을 개별 저장하여, 내용 때문에 늘어난 열만 커지고 다른 열 폭은 유지.

- 표 구성요소 추가. 삽입 시 기본 가로 너비(기본 프리셋 400px), 행/열 수, 테두리 색상을 지정 가능. 높이는 직접 지정하지 않으며 한 줄 높이를 기본으로 사용.
- 표는 설정된 기본 너비를 최소 크기로 유지하고, 셀의 한 줄 내용이 길어지면 자동으로 실제 가로 너비가 늘어남. 명시적 개행만 셀 높이를 늘림.
- 표 편집 창에서 셀별 배경색/텍스트, 열 최소 너비, 행/열 추가·제거, 사각형 셀 병합/병합 해제 지원.
- 현재 실제 표 너비를 기본 너비로 동기화하는 기능 추가. 열 너비 조정 시 기본 너비도 열 최소 너비 합계로 갱신.
- 표도 캐릭터 카드/설명 상자와 동일하게 한 번 클릭 또는 같은 높이 여백 클릭 시 오른쪽 아래 편집 커서를 활성화하고, 더블클릭 시 전용 편집 창을 엶.

## 3.34
- 글머리/번호 버튼이 선택 범위를 강제로 블록 경계까지 확장하면서 Chromium에서 목록 명령이 무시되던 문제 수정. 실제 선택 범위를 유지한 채 브라우저의 줄 단위 목록 명령을 실행하도록 변경.
- 목록 적용/해제 뒤 생길 수 있는 잘못된 중첩 `<p><ul>...</ul></p>` 및 루트 텍스트를 정상 블록 구조로 자동 정리.

- 편집기와 정적 열람 사이트의 시각 디자인을 3.15 기준으로 되돌림.
- Duels 3.1717의 캐릭터 설명/툴팁과 동일한 UI 폰트 스택(SUIT → Pretendard → Wanted Sans → Noto Sans KR 등)을 편집기와 열람 사이트에 적용.
- 3.17의 GitHub PUT/DELETE 409 stale SHA 자동 재시도 로직 유지.
- 카테고리 정렬 시 미분류를 항상 가장 아래에 배치.
- 3.15 디자인을 유지하면서 모바일 폭 넘침/이미지/편집 폼 레이아웃 안정화.
- 표 기능은 계속 제거된 상태 유지.



## 3.34
- 글머리/자동 번호 매기기 버튼이 문자 선택 범위가 아니라 현재 줄 단위로 적용되도록 변경. 여러 줄을 선택했을 때만 선택한 줄들을 함께 목록으로 변환.
- 업데이트 Git 동기화에서 `wiki/`와 `media/`를 제품 파일과 분리. 기존 GitHub 저장소가 있으면 원격의 위키 문서/미디어를 먼저 복원하고 에디터·정적 사이트·워크플로·동기화 스크립트만 교체/커밋하도록 변경.
- 새 저장소를 처음 만드는 경우에만 기본 `wiki/_root.json`, `wiki/categories.json`, `media/images/.gitkeep`을 초기 데이터로 추가.

## 3.32
- 홈 > 단락에 Word식 가운데점 글머리 목록과 자동 번호 목록을 명확히 제공. Enter 시 다음 항목이 자동 생성되며 빈 항목에서 Enter를 다시 누르면 목록을 종료.
- 편집기와 정적 열람 사이트에서 가운데점 목록/자동 번호 목록의 표시 스타일을 통일.
- 목차의 번호 영역에 고정 폭을 주던 방식을 제거하고 깊이별 들여쓰기 + 고정 간격으로 변경해 4.1 / 4.1.1 같은 하위 목차에서 제목 앞 공백이 비정상적으로 커지는 문제 수정.


## 3.31
- 캐릭터 카드와 설명 상자를 한 번 클릭하면 해당 요소 바로 아래의 실제 텍스트 문단을 활성화하도록 변경.
- 요소 바로 아래에 이미 빈 입력 문단이 있으면 중복 개행을 만들지 않고 기존 문단을 재사용.
- 캐릭터 카드 편집을 설명 상자와 동일하게 더블클릭으로 열도록 변경.
- 더블클릭 시 앞선 단일 클릭 처리를 취소해 빈 문단이 생긴 뒤 편집창이 열리는 현상을 방지.

## 3.20

- 캐릭터 카드 구성요소를 프로필 폼이 아닌 범용 이미지+텍스트 카드로 단순화.
- 원형 초상화/스탯/스킬/칭호/호버 툴팁/호버 확대 효과 제거.
- 듀얼즈 카드처럼 이미지가 카드 배경 전체를 채우고 아래쪽으로 어두운 그라데이션을 적용.
- 기존 3.19 카드 데이터는 열 때 범용 텍스트 카드로 자동 변환.

## 3.19
- 삽입 리본에 듀얼즈식 캐릭터 카드와 설명 상자 구성요소 추가.
- 캐릭터 카드는 초상화/이름/대표색/체력/이동속도/설명/스킬 설명을 저장하고 hover/focus 설명 툴팁을 표시.
- 편집 화면에서 구성요소 더블클릭으로 재편집/삭제 가능.
- 정적 열람 사이트에도 동일한 구성요소 스타일 적용.
